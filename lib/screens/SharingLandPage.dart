import 'package:flutter/material.dart';

class SharingLand extends StatefulWidget {
  @override
  _SharingLandState createState() => _SharingLandState();
}

class _SharingLandState extends State<SharingLand> {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      // appBar: new AppBar(
      //   title: new Text('Sharing Land'),
      // ),
      body: new Center(
        child: new Text("This is Sharing Land Page !! "),
      ),
    );
  }
}
